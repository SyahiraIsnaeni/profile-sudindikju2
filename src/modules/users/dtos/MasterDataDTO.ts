export interface UserMasterDTO {
  id: number;
  name: string;
  email: string;
  status: number;
}

export interface RoleMasterDTO {
  id: number;
  name: string;
}

export interface PermissionMasterDTO {
  id: number;
  name: string;
}